import { StyleSheet, Dimensions } from "react-native";

const styles = StyleSheet.create({
    container: {
        flexDirection: "row",
        alignItems: "center",
        overflow: "hidden",
    },
    itemControl: {
        flexDirection: "row",
        overflow: "hidden",
    },
});

export default styles;